# DOMAINE est défini dans .env, ex. api.12-34-56-78.sslip.io (sans nom de domaine)
# ou api-finances.mondomaine.com. Caddy obtient le certificat HTTPS automatiquement.
{$DOMAINE} {
    reverse_proxy app:8000
}
